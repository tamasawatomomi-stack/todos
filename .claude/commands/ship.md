プロジェクトをビルドし、テストを実行し、すべて成功したら変更をコミットしてプッシュする。

手順:
1. `npm run build` を実行してビルドが成功することを確認する
2. テストフレームワークが設定されている場合は `npm test` を実行する
3. ビルド・テストがすべて成功した場合のみ、git add・commit・push を行う
4. いずれかのステップが失敗した場合は処理を中断し、エラー内容を報告する
